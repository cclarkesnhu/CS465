1.Compare and contrast the types of frontend development you used in your full stack project, including Express HTML, JavaScript, and the single-page application (SPA).

The front end of this project, the first type I used was Express HTML. The front-end customer facing portion wsas consisted of static HTML pages, images, and handlebars for user experience purposes. Because of this, it wasn't possible to update information because of the static
composition of the website. Given the capacity to need to change tirp information, new deals, sales, etc. We would need a different system. Javascirpt allowed us to dynamically change the data, update it when we stored information on MongoDB. Lastly, we used the Single-Page application, Angular, that we used to create an admin page
for the web application. THis included user certification, authorization checks. Commonly I didn't need to reset the server if I was making changes to it, save for a few instances. But it also prevented me from going back.

2. Why did the backend use a NoSQL MongoDB database?

This is for storing our data in an unstructured format. This allows us to use a wide variety of formats like JSON files.
3. How is JSON different from Javascript and how does JSON tie together the frontend and backend development pieces?

Javascript is a programming language, whereas JSON is a data format that is used to send data to and from the web page. JSON is a translator between the front end and the back end, because different front-ends and back-ends, might use different programming languages.

4. Provide instances in the full stack process when you refactored code to improve functionality and efficiencies, and name the benefits that come from reusable user interface (UI) components.

One method that we used was modifying the static JSON to create a template from the trip cards, and the trip listing. Allowing us to add, edit, and remove information from our MongoDB without having to rewrite the entire webpage

5. Methods for request and retrieval necessitate various types of API testing of endpoints, in addition to the difficulties of testing with added layers of security. Explain your understanding of methods, endpoints, and security in a full stack application.

The methods in a full-stack application allows us to work with the application to access, edit database information, as well as retrieve information. The main methods that we used were GET, POST, PUT, and DELETE. The API endpoints are how the client communicates with the server using these HTTP Methods. Security is used by authenticated a user, giving them a valid JWT token, so they could access the endpoints.

6. How has this course helped you in reaching your professional goals? What skills have you learned, developed, or mastered in this course to help you become a more marketable candidate in your career field?

7. This course has helped me a bit in my professional goals because it has given me more skills for web development. And insight on what it takes to make a full-stack application. This helps me show how to work with a MEAN stack, creating dynamic web pages, working with NoSQL databases within webpages.
8. This will inevitably help me get my first steps in working with web design, which I have personally expressed interest in.
